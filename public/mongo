$gt:초과
$gte:이상
$lt:미만
$lte:이하
$ne: 같지 않음
$or: 또는
$in: 배열 요소 중 하나

#정렬 sort
-1:내림차순
 1:오름차순

#개수 설정 limit

 db.users.find({},{_id:0, name:1,age:1}).sort({age:-1});db
    db.users.find
#skip() 건너뜀

#관리자 계정
use admin하고
db.createUser({user:'', pwd:'', roles:['root']})로 꼐정 만들수 있음

#데이터베이스 만들기(디비를 생성해도 데이터를 최소 한 개 이상 넣지 않으면 보이지 않음.)
use 이름

#데이터베이스 목록 확인
show dbs

#현재 사용중인 데이터베이스 확인
db

#컬렉션 생성
db.createCollection();
#컬렉션 확인
show collections


